import { formData } from "../types/formdata";

export const calculateCarbonFootprint = (inputs: formData): number => {
  console.log("do calulcations", inputs);
  return 1;
};

export default calculateCarbonFootprint;
